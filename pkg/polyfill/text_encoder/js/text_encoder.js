class TextEncoder {
  constructor() {
    this.encoding = 'utf-8'
  }

  encode(str) {
    // TODO

    // throw 'TODO: TextEncoder.encode'
  }

  encodeInto(str, arr) {
    // TODO

    // throw 'TODO: TextEncoder.encodeInto'
  }
}
